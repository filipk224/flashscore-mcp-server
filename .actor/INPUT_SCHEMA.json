{
  "title": "Flashscore MCP Server Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "idleTimeoutSecs": {
      "title": "Idle timeout (seconds)",
      "type": "integer",
      "description": "How long the Actor may stay idle in Standby mode before being shut down. Use a high value for always-on MCP access.",
      "default": 3600,
      "minimum": 60,
      "maximum": 86400
    }
  },
  "required": []
}
